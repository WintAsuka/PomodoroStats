apply plugin: "com.android.application"

android {
    compileSdkVersion 30

    defaultConfig {
        applicationId "com.codex.pomodorostats"
        minSdkVersion 23
        targetSdkVersion 30
        versionCode 1
        versionName "1.0.0"
    }
}
